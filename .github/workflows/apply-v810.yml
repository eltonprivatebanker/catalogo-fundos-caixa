name: Aplicar Catalog Risk V810

on:
  push:
    branches:
      - 'Testes-da-versão-01'
    paths:
      - '.github/workflows/apply-v810.yml'

permissions:
  contents: write

jobs:
  aplicar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 'Testes-da-versão-01'
          fetch-depth: 0

      - name: Atualizar index para V810
        shell: python
        run: |
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = s
          css_tag = '<link rel="stylesheet" href="catalog-risk-v810.css?v=catalog-risk-v810"/>'
          js_tag = '<script src="catalog-risk-v810.js?v=catalog-risk-v810" defer></script>'
          if css_tag not in s:
              s = s.replace('</head>', css_tag + '\n</head>')
          if js_tag not in s:
              s = s.replace('</body>', js_tag + '\n</body>')
          if s == old:
              raise SystemExit('V810 já estava carregada no index.html')
          p.write_text(s, encoding='utf-8')

      - name: Commit V810
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'V810: aprimorar seletor de perfil de risco'
          git push origin HEAD:'Testes-da-versão-01'
